'use strict';

/**
 * detalle-orden-item controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::detalle-orden-item.detalle-orden-item');
